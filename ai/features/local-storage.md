# Features: Store to SQLITE

## Current Problem
- Once the application close the data is lost, and the user has to start from scratch
---

## FIX
- Implement a local storage solution using SQLite to persist user data across sessions.

## Future Ideas
Use a library like `react-native-sqlite-storage` to manage the SQLite database in the React Native application.
